mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.lt
spring:
  jmx:
    default-domain: cut-cloud-war-1.0-SNAPSHOT
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/sms
    driver-class-name: com.mysql.jdbc.Driver