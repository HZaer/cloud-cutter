mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.lt
endpoints:
  jmx:
    unique-names:true
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/sms
    driver-class-name: com.mysql.jdbc.Driver